# k8s/monitoring/grafana/service.yml
apiVersion: v1
kind: Service
metadata:
  name: grafana-service
  namespace: sheren04-dev
  labels:
    app: grafana
spec:
  selector:
    app: grafana
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
  type: ClusterIP
