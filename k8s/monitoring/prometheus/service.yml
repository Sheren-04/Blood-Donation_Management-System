# k8s/monitoring/prometheus/service.yml
apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  namespace: sheren04-dev
  labels:
    app: prometheus
spec:
  selector:
    app: prometheus
  ports:
  - protocol: TCP
    port: 9090
    targetPort: 9090
  type: ClusterIP
